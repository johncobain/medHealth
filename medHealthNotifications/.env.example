# Example environment variables for medHealthNotifications service
# Create a .env file in the medHealthNotifications directory and populate it with your own values
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-password